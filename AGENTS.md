# AGENTS.md

## Project Overview

"오늘의 태그"는 사용자의 하루를 기록하고, 축적된 데이터를 기반으로 자신의 생활 패턴을 돌아볼 수 있도록 만든 개인 회고 웹앱이다.

사용자는 감정, 활동, 몸 상태, 자기 평가, 한 줄 기록을 남길 수 있으며, 기록이 쌓이면 통계와 회고 기능을 통해 자신의 패턴을 확인할 수 있다.

---

## Tech Stack

- HTML
- CSS
- JavaScript (Vanilla JS)
- LocalStorage
- Vercel Serverless Function (뉴스 RSS 자동 갱신)

외부 프레임워크나 라이브러리 의존성을 최소화한다.

---

## Core Features

현재 구현된 주요 기능:

### Daily Record
- 오늘의 기분 기록
- 활동 태그 선택
- 몸 상태 기록
- 자기 상태 기록
- 나를 칭찬하기
- 선택한 칭찬 항목별 짧은 메모 작성
- 한 줄 메모 작성
- 날짜 선택을 통한 과거 기록 작성 및 수정

### History
- 최근 7일 기록 조회
- 기록 상세 보기
- 과거 날짜 기록 수정 및 삭제
- 과거 데이터 유지 및 호환

### Statistics
- 7일 / 30일 기준 데이터 분석
- 기록 일수 및 기록률 표시
- 가장 많이 선택한 기분 분석
- 가장 많이 한 활동 분석
- 자주 기록된 몸 상태 분석
- 칭찬 횟수 분석
- 기분 변화 흐름 시각화

### Smart Weekly Review
- 저장된 기록 데이터를 기반으로 자동 회고 제공
- 사용자 기록 패턴에 따른 짧은 인사이트 제공
- 별도의 AI API 호출 없이 브라우저 내부 데이터 분석 방식 유지

### Daily News
- 기록 날짜에 맞는 뉴스 헤드라인 표시
- 페이지 접속 시 Vercel 함수에서 오늘 뉴스 자동 갱신
- 분야별 균형 선별 및 정치 뉴스 최대 1건 제한
- 로컬 실행에서는 저장된 뉴스 데이터로 대체

---

## Development Principles

### 1. Existing Functionality First

새로운 기능 추가 시 기존 기능을 깨뜨리지 않는다.

특히 아래 기능은 반드시 유지한다.

- 기록 저장
- 기록 불러오기
- 선택 날짜 기준 기록 작성, 수정, 삭제
- 최근 기록 표시
- 통계 계산
- 다크모드
- 모바일 대응

---

## Code Modification Rules

- 작업 전 기존 코드 구조를 먼저 분석한다.
- 필요한 부분만 수정한다.
- 전체 파일을 불필요하게 재작성하지 않는다.
- 기존 변수명과 데이터 구조를 최대한 유지한다.
- 작은 기능 추가는 기존 구조 안에서 해결한다.
- 새로운 라이브러리 추가는 반드시 필요한 경우에만 한다.
- 뉴스 자동 갱신은 API 키 없이 Vercel 함수와 RSS를 사용한다.

---

## Data Management Rules

현재 데이터 저장 방식은 LocalStorage이다.

규칙:

- 기존 저장 데이터와 호환성을 유지한다.
- 기존 사용자의 기록이 삭제되거나 손상되지 않도록 한다.
- 데이터 구조 변경 시 마이그레이션을 고려한다.
- 통계 기능은 저장된 기록 데이터를 기반으로 동작해야 한다.

---

## UI/UX Guidelines

디자인 방향:

- 따뜻하고 편안한 감성 유지
- 사용자가 부담 없이 하루를 기록할 수 있는 경험 우선
- 복잡한 기능보다 직관적인 사용성을 우선

수정 시:

- 기존 컬러 시스템 유지
- 기존 카드형 UI 유지
- 모바일 환경에서도 정상 작동하도록 한다.
- 새로운 화면은 기존 디자인과 자연스럽게 연결한다.

---

## Smart Review Guidelines

회고 기능 수정 시:

- 긴 대화형 AI 챗봇 형태로 확장하지 않는다.
- 사용자의 기록 데이터를 기반으로 짧고 의미 있는 피드백을 제공한다.
- 불필요한 API 호출 구조를 만들지 않는다.
- 사용자가 실제로 도움이 된다고 느낄 수 있는 인사이트를 우선한다.

---

## Future Development Direction

향후 확장 가능 기능:

- 더 다양한 데이터 시각화
- 사용자별 태그 관리
- 기록 검색 기능
- 데이터 내보내기/백업
- PWA 지원
- AI 기반 개인 맞춤형 회고 기능

---

## Development Goal

단순한 일기 앱이 아니라,

"사용자가 자신의 하루를 기록하고, 데이터를 통해 자신을 이해할 수 있는 개인 회고 서비스"

로 발전시키는 것을 목표로 한다.

새로운 기능을 추가할 때는 항상 다음 질문을 우선한다.

"이 기능이 사용자의 기록 경험과 자기 이해에 실제 가치를 더하는가?"
